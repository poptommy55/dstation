@@PRETTY_NAME@@  安装说明
@@PLUGIN_NAME@@  @@VERSION@@
================================================================

一句话
    解压 → 双击「一键安装.cmd」→ 重启 DSH。就装好了。


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 一、怎么装
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

① 把这个压缩包**完整解压**到一个文件夹
   （不要只解压一部分，也不要直接在压缩包里双击运行）

② 双击  一键安装.cmd

③ 看到一个黑窗口，跑完会显示「安装完成 ✓」
   如果显示「安装包缺少以下文件」，说明没解压完整，重新解压再试。

④ **重启一次 DSH**
   插件是在 DSH 启动的时候装载的，所以装完必须重启才生效。


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 二、装完在哪里看到
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

@@WHERE_TO_LOOK@@

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 三、怎么卸载
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

双击  一键卸载.cmd  ，然后同样重启一次 DSH。

卸载会移除插件和启动清单里的登记。插件目录默认保留，
如果也想一起删掉，打开 PowerShell 运行：

    powershell -ExecutionPolicy Bypass -File uninstall.ps1 -RemovePackage


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 四、它做了什么（你可以自己核对）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

安装脚本只做四件事，全部在**你自己的 DSH 目录里**，不改系统、不联网：

  1. 把 plugin\ 里的文件复制到
         <DSH主目录>\plugins\@@PLUGIN_NAME@@\
  2. 在 profile 里建一个链接指向上面那个目录
  3. 把插件登记进 DSH 的启动清单（会先备份 profile\package.json）
  4. 检查有没有「重复挂载」冲突

安装脚本**不会**：装驱动、改注册表、写系统目录、下载任何东西、开机自启。

想自己看代码的话，install.ps1 / uninstall.ps1 就是普通文本，用记事本打开即可。


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 五、出问题了
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

【双击没反应 / 闪一下就没了】
    说明 .cmd 被拦了。右键 一键安装.cmd → 以管理员身份运行试试，
    或者手工：在这个文件夹里按住 Shift 右键 →「在此处打开 PowerShell」，
    然后运行：
        powershell -ExecutionPolicy Bypass -File install.ps1

【提示「找不到 DSH 主目录」】
    脚本会列出它试过哪些位置。如果你的 DSH 装在别处，手工指定：
        powershell -ExecutionPolicy Bypass -File install.ps1 -DshHome "你的DSH主目录"

【提示「profile 不明确」】
    你有多个 profile，脚本不知道装哪个。加上名字：
        powershell -ExecutionPolicy Bypass -File install.ps1 -Profile web

【提示「双重挂载」警告】
    你的 DSH 里已经用「手工挂载」的方式装过这个插件。
    两个通道同时存在会让 DSH 起不来，所以脚本会警告你。
    按它给出的路径打开那个 cordis.patch.yml，删掉对应的那段再重启。

【装完了但界面没变化】
    99% 是没重启。插件装载发生在 DSH 启动时。
    重启之后还是没有的话，把安装时黑窗口里的文字发给作者。

【想彻底回到装之前】
    install.ps1 在改 profile 之前会备份：
        <profile目录>\package.json.bak-@@PLUGIN_NAME@@
    把它覆盖回去，然后重启。


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
 六、这个包里有什么
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    一键安装.cmd        双击这个
    一键卸载.cmd        双击这个
    install.ps1         安装脚本（可以打开来看）
    uninstall.ps1       卸载脚本
    安装说明.txt        本文件
    MANIFEST.json       逐文件校验清单（安装前会核对，防止包损坏）
    plugin\             插件本体
        （共 @@FILE_COUNT@@ 个文件，@@TOTAL_SIZE@@）

安装包构建时间：@@BUILT_AT@@

================================================================
共 @@FILE_COUNT@@ 个文件 · @@TOTAL_SIZE@@
