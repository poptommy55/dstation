============================================================
  智能体安装包 · 使用说明
============================================================

这个包里是 @@AGENT_COUNT@@ 个 D-STATION 智能体：

  @@AGENT_NAMES@@

  智能体 id ：@@AGENT_IDS@@
  打包时间  ：@@BUILT_AT@@
  文件数量  ：@@FILE_COUNT@@ 个（共 @@TOTAL_SIZE@@）
  来源机器  ：@@SOURCE_HOME@@


------------------------------------------------------------
  怎么装（一分钟，不用命令行）
------------------------------------------------------------

  1. 把整个压缩包**完整解压**到一个文件夹。
     （不要在压缩包里直接双击 —— 脚本要读同目录的其他文件。）

  2. 双击「一键安装.cmd」。

  3. 看窗口里的输出：出现 [OK] 就是装好了。
     如果出现 [X]，窗口里会写清楚原因。

  4. 打开 D-STATION，点侧边栏底部的「智能体工作台」。
     新装的智能体会出现在 SUPER AGENTS 分区里。
     没看到的话，把 D-STATION 完全退出再启动一次。


------------------------------------------------------------
  它到底做了什么
------------------------------------------------------------

  只是把智能体目录复制到：

      <D-STATION 数据目录>\.agent-presets\<智能体id>\

  这个脚本**不会**碰：
    · 对方已经装好的插件
    · profile 配置、node_modules、符号链接
    · 任何 API KEY 或登录状态

  所以装智能体是安全的、可撤销的：删掉 .agent-presets 里对应的
  文件夹就等于卸载。

  安装前会逐文件校验 sha256；如果包在传输中损坏或被改动过，
  脚本会直接中止，不会装出一个半成品。


------------------------------------------------------------
  装之前请先看：这个智能体需要什么
------------------------------------------------------------

@@DEPS_TEXT@@


------------------------------------------------------------
  覆盖安装 / 卸载
------------------------------------------------------------

  覆盖安装
    如果对方已经有同 id 的智能体，脚本会先把旧版本备份到：

        <D-STATION 数据目录>\agent-preset-backups\<id>-<时间戳>\

    然后再放新版本。想保留旧版不覆盖，用：

        powershell -ExecutionPolicy Bypass -File install.ps1 -KeepExisting

    （备份目录故意放在 .agent-presets 外面 —— 放里面会被
      D-STATION 当成一个额外的智能体扫出来，列表里就重复了。）

  卸载
    直接删掉 <D-STATION 数据目录>\.agent-presets\<智能体id>\ 即可。


------------------------------------------------------------
  装不上怎么办
------------------------------------------------------------

  「找不到 D-STATION 数据目录」
      脚本会自动找几个常见位置。找不到时手工指定：

          powershell -ExecutionPolicy Bypass -File install.ps1 -DshHome "C:\你的\D-STATION\home"

      小技巧：在 D-STATION 里随便开一个会话，看它的工作目录，
      往上找能看到 profiles 文件夹的那一层就是数据目录。

  「安装包缺少文件 / 内容与清单不符」
      压缩包没解压完整，或传输过程中损坏了。请让对方重新导出、
      用别的方式（网盘/U盘）重新传一遍，并确认解压完整。

  「装好了但工作台里看不到这个智能体」
      1) 先完全退出 D-STATION 再启动（智能体目录在启动时扫描）；
      2) 确认 .agent-presets\<id>\ 下有 agent.cordis.yml；
      3) 如果这个智能体依赖某个插件而对方没装，它会起不来 ——
         看上面「这个智能体需要什么」那一段。

  「智能体能打开，但功能报错」
      多半是依赖没满足：缺插件、缺 API KEY、或文中写死的绝对路径
      在对方机器上不存在。对照上面的依赖清单逐条检查。

============================================================
